# Configurações do servidor
PORT=3333
WORKER_PORT=3334

# Banco de dados PostgreSQL
POSTGRES_USER=wisiex_user
POSTGRES_PASSWORD=senha_segura_para_postgres
POSTGRES_DB=wisiex_db
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Redis
REDIS_URL=redis://redis:6379

# Segurança
JWT_SECRET=chave_muito_segura_para_jwt_tokens_pelo_menos_32_caracteres

# Observe que este é apenas um arquivo modelo (env.example)
# Você deve criar um arquivo .env real no servidor com valores seguros
# O arquivo .env real NUNCA deve ser commitado no Git 